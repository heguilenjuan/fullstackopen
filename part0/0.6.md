squeceDiagram
    participan browser
    particiapnt server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: 201 created + message (JSON)
    deactivate server

    Note right of browser: JavaScript update state (notes array)
    and re-renders the DOM.